# Base URL del backend para las llamadas del frontend.
# Deja el valor en blanco para usar el mismo origen (por ejemplo cuando el frontend se sirve detrás de Nginx).
# Para desarrollo local con `php artisan serve` en el puerto 8000 usa:
# VITE_API_BASE_URL=http://localhost:8000
VITE_API_BASE_URL=
