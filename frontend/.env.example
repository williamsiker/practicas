# Base URL del backend para las llamadas del frontend.
# Si no se define, en modo desarrollo se usará http://localhost:8080 automáticamente
# (puedes cambiar el puerto estableciendo VITE_API_DEV_PORT).
# Deja el valor en blanco para usar el mismo origen al servir la aplicación en producción.
VITE_API_BASE_URL=

# Puerto opcional a utilizar en desarrollo (ej. 8000 si usas `php artisan serve`).
# VITE_API_DEV_PORT=8000
